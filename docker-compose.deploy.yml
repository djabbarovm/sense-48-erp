# H-06: оверлей для доступа по IP без домена (HTTP на :80).
# Когда появится домен — убрать этот оверлей и поставить caddy/nginx c TLS.
services:
  web:
    ports:
      - '80:3000'
    volumes:
      - ./data:/app/data
